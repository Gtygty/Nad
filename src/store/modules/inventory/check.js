import {} from "@/api/inventory/check";
const check = {
  state: {},
  mutations: {},
  actions: {
    //获取列表
    // async GetClinicList({ dispatch, commit }, params) {
    //   let res = await getClinicList(params);
    //   return res;
    // },
  }
};

export default check;
